[pytest]
addopts = -q -m "not stress and not benchmark"
python_files = test_*.py
markers =
    mvp: tests that must pass for MVP
    stress: heavy load / scaling tests (deselect by default)
    benchmark: performance regression baselines
